# 1.1.1 alpha-to-beta physical mail demonstration

Milestone baseline: `54b5b274ec28986027f163389a26d20e340232b6`

This record is populated only from fresh tests originating on the five required
physical endpoints and independent approved-client tests for the two required
administrative mailboxes. Correlation identifiers are non-secret. Credentials,
message contents, AUTH payloads, and private configuration are excluded.

## Required endpoints

| Identity | Endpoint | Test timestamp | TLS | Submission | Delivery | Reply | Retrieval | External relay | Final classification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

Required identities are `hv-lore@home.arpa`, `hv-katra@home.arpa`,
`hv-matrix@home.arpa`, `ws-matriarch@home.arpa`, `ws-hadrian@home.arpa`,
`admin@home.arpa`, and the canonical existing `cluster-admin@home.arpa`.
`cluster_admin@home.arpa` must not be created.

## Deferred endpoints

- `ws-alpha@home.arpa`: operator-deferred; cohabitates with verified
  `ws-matriarch`; not classified as verified.
- `ws-wowzerwin@home.arpa`: operator-deferred and temporarily unserviceable;
  not classified as verified.

The milestone remains `VERSION=1.1.1-alpha` and `BETA_ELIGIBLE=false` until all
seven required rows are present and classified `SEND/RECEIVE VERIFIED`.
Satisfying this blocker does not itself change the release label to beta.
