# Stalwart 0.16.15 home.arpa compatibility and local foundation

Date: 2026-08-01  
Starting repository state: `1baef8f9d2fedb29fec6681019f3ae0872e5ffa4`  
Packet commit: `192a058`  
Target: `mail-core-9000` / `mail.home.arpa`  
Result: **HOME.ARPA ACCEPTED — LOCAL FOUNDATION CONTINUING**

## Verified patch artifact

The official `stalwartlabs/stalwart` v0.16.15 release supplied:

```text
asset:          stalwart-x86_64-unknown-linux-gnu.tar.gz
URL:            https://github.com/stalwartlabs/stalwart/releases/download/v0.16.15/stalwart-x86_64-unknown-linux-gnu.tar.gz
size:           39929282 bytes
SHA-256:        6f23c6217f0f9710bafde889b7efbc349c7cfa613c4b14b8b365245799373676
bundle URL:     https://github.com/stalwartlabs/stalwart/releases/download/v0.16.15/stalwart-x86_64-unknown-linux-gnu.tar.gz.sigstore.json
bundle size:    10950 bytes
bundle SHA-256: 0beaf90f90574b521eced4230ade0cb9019dc64c870eb2b673413d4cb4a15e18
binary SHA-256: 27d8b065bb1f04662238004d8b1f220defa951c0be82ea153a46433e7ccc0702
acquired:       2026-08-01T09:06:51-04:00
```

Offline `cosign` verification passed with the exact tagged workflow identity
and GitHub Actions OIDC issuer. The archive contained one safe regular relative
entry. The extracted and installed binary both reported `0.16.15`. No installer,
container, moving release, mirror, or replacement artifact was used.

## Binary replacement

Preflight confirmed the accepted stopped/disabled 0.16.4 installation, absent
recovery state and listeners, healthy `/srv/stalwart`, absent
`/var/lib/stalwart`, and healthy guest/host control planes.

The prior binary was copied to the protected root-only path
`/usr/local/libexec/stalwart-backup/0.16.4/stalwart`. Its SHA-256 is
`8d7ae63daf10aecd7175f6f6cdc4e1de3560c1724468eb501164129c12ac72d2`
and it reports `0.16.4`. Only `/usr/local/bin/stalwart` was replaced; its
installed hash and version match the verified v0.16.15 artifact.

## Compatibility result

The accepted temporary `PrivateNetwork=yes` recovery unit again proved:

- a distinct network namespace;
- only an up loopback interface;
- no external interface, veth, route, NAT, forwarding, or DNS dependency;
- recovery port 8080 reachable only inside that namespace;
- no recovery reachability through `192.168.100.199`.

The v0.16.15 schema describe completed. The existing sanitized plan exactly
matched the committed template hash. Dry-run reported two singleton updates,
one creation, three upsert groups, nine objects, and no reconcile or destroy.

The real Domain-only compatibility probe succeeded:

```text
Domain upsert: home.arpa
created: 1
failed: 0
```

This proves Stalwart v0.16.15 supports the fixed `home.arpa` architecture. No
domain substitution or validator bypass was used.

## Schema-derived corrections

Three representation corrections were required and independently derived from
the live schema and exact v0.16.15 source:

1. omitted an optional empty Tracer `events` set after the server rejected its
   empty array form;
2. encoded `Map<SocketAddr>` listener binds as address-to-true objects;
3. encoded `List<Credential>` as a numeric-keyed object.

Each corrected plan passed dry-run. The completed apply created exactly one
log tracer, three loopback listeners, and four accounts. The committed
placeholder plan contains no secrets.

## Accepted local foundation

Live schema queries confirmed:

- enabled, non-relaying domain `home.arpa`;
- default hostname `mail.home.arpa` and default domain `home.arpa`;
- RocksDB under `/srv/stalwart/data`;
- filesystem blobs under `/srv/stalwart/blobs`;
- local logs under `/srv/stalwart/log`;
- `postmaster`, `test-sender`, and `test-receiver` with User role;
- `cluster-admin` with Administrator role;
- exactly `http-local` on `127.0.0.1:8080`, `submission-local` on
  `127.0.0.1:1587`, and `imap-local` on `127.0.0.1:1143`;
- no DNS provider object and no Fastmail relay;
- `MtaStageRcpt.allowRelaying` fixed to unconditional false.

Four distinct random credentials exist only in the protected ignored guest
directory. Mode and pairwise-distinct checks passed without emitting values.

## Acceptance tests

- The permanent service is enabled, active, and still uses the accepted
  mount-guarded hardened unit.
- A temporary loopback-only plaintext-auth exception was used solely for the
  protocol tests and removed afterward.
- Authenticated `test-sender@home.arpa` delivered one local construction
  message to `test-receiver@home.arpa`.
- The receiver authenticated and retrieved the message through loopback IMAP.
- The message survived a Stalwart restart and a guest reboot.
- Unauthenticated external relay was rejected.
- After setting `allowRelaying` false, authenticated delivery to an
  unmistakably fake external recipient was rejected at RCPT before DATA.
- No external message was sent.
- Final SMTP auth advertises only OAuth mechanisms on unencrypted loopback;
  PLAIN/LOGIN are limited to TLS by the restored policy.
- Final IMAP `allowPlainTextAuth` is false.
- The populated mount-guard test stopped Stalwart, unmounted and runtime-masked
  `/srv/stalwart`, and proved the permanent service stayed inactive with
  `ConditionResult=no` and wrote no root-filesystem fallback state. The accepted
  filesystem and service were restored successfully.
- `/var/lib/stalwart` remains absent.

## Final health and boundary

`/srv/stalwart` is mounted from `/dev/vdb1` as XFS with `202069696512` bytes
available. Stalwart state paths remain mode 0750 and owned by
`stalwart:stalwart`. The only Stalwart listeners are the three loopback sockets;
none binds to `192.168.100.199`, a wildcard address, port 25, 587, or 993.

SSH, qemu-guest-agent, guest networking, and package DNS resolution are healthy.
The VM remains 2 vCPU, 4096 MiB, and libvirt-autostart disabled. The Matriarch
pool mount and bridge connections are healthy; its default route remains on
`eno1`. No DNS, RouterOS, firewall, NetworkManager, libvirt, host-storage, TLS,
Fastmail, public listener, or external-mail mutation occurred.

Trusted private-lab-CA issuance and DNS publication remain separate acceptance
gates before Lab-10 submission and IMAPS.

**HOME.ARPA ACCEPTED — LOCAL FOUNDATION CONTINUING**
