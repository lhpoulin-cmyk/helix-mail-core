# Production inventory

`values.env` is deliberately ignored. Copy the example only after an approved
implementation packet establishes every value from current evidence. A render
with any `UNRESOLVED_*` value fails. Never put Fastmail or account credentials
in this directory.

