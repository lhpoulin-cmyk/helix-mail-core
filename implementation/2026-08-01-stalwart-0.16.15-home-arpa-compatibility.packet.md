# Stalwart 0.16.15 home.arpa compatibility packet

Status: authorized patch upgrade and compatibility test  
Starting repository state: `1baef8f9d2fedb29fec6681019f3ae0872e5ffa4`  
Target: `mail-core-9000` / `/usr/local/bin/stalwart`

## Boundary

Replace only the stopped Stalwart v0.16.4 server binary with the verified
v0.16.15 patch binary, then test `home.arpa` through the accepted isolated
recovery namespace. Preserve the CLI v1.0.12, account, ownership, mount,
permanent unit, datastore config, networking, DNS, TLS, credentials, and
Fastmail state. Do not use an installer or apply a plan unless the initial
compatibility apply attempt accepts its first `home.arpa` Domain operation.

## Pinned official artifact

```text
release:        v0.16.15
published:      2026-07-27T05:58:15Z
asset:          stalwart-x86_64-unknown-linux-gnu.tar.gz
URL:            https://github.com/stalwartlabs/stalwart/releases/download/v0.16.15/stalwart-x86_64-unknown-linux-gnu.tar.gz
size:           39929282 bytes
SHA-256:        6f23c6217f0f9710bafde889b7efbc349c7cfa613c4b14b8b365245799373676
bundle:         stalwart-x86_64-unknown-linux-gnu.tar.gz.sigstore.json
bundle URL:     https://github.com/stalwartlabs/stalwart/releases/download/v0.16.15/stalwart-x86_64-unknown-linux-gnu.tar.gz.sigstore.json
bundle size:    10950 bytes
bundle SHA-256: 0beaf90f90574b521eced4230ade0cb9019dc64c870eb2b673413d4cb4a15e18
identity:       https://github.com/stalwartlabs/stalwart/.github/workflows/ci.yml@refs/tags/v0.16.15
OIDC issuer:    https://token.actions.githubusercontent.com
binary SHA-256: 27d8b065bb1f04662238004d8b1f220defa951c0be82ea153a46433e7ccc0702
acquired:       2026-08-01T09:06:51-04:00
```

The exact URLs came from the official GitHub v0.16.15 release metadata. The
protected archive contains exactly one regular relative entry named
`stalwart`; no traversal, absolute path, device entry, or unrelated payload is
present. After locally adding execute permission to the temporary extraction,
the binary reports `0.16.15`.

The already installed Debian `cosign` 2.5.0-2+b4 verified the bundle offline
with exact tag identity and issuer, without disabling SCT or transparency
checks:

```sh
cosign verify-blob --new-bundle-format --offline \
  --bundle stalwart-x86_64-unknown-linux-gnu.tar.gz.sigstore.json \
  --certificate-identity 'https://github.com/stalwartlabs/stalwart/.github/workflows/ci.yml@refs/tags/v0.16.15' \
  --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' \
  stalwart-x86_64-unknown-linux-gnu.tar.gz
```

## Immediate preflight and replacement

Require clean committed packet state and reconfirm:

- hostname and domain identity;
- server `0.16.4`, CLI `1.0.12`, service disabled/inactive;
- no recovery process/unit/environment and no Stalwart listener;
- `/srv/stalwart` is the accepted `/dev/vdb1` XFS mount with healthy capacity;
- `/var/lib/stalwart` absent;
- no Domain, Account, or NetworkListener object was created by the failed
  v0.16.4 attempt;
- permanent unit and minimal config remain unchanged.

Copy `/usr/local/bin/stalwart` to protected root-only
`/usr/local/libexec/stalwart-backup/0.16.4/stalwart`, record its SHA-256, and
require the backup to report `0.16.4`. Install only the verified extracted
v0.16.15 binary atomically at `/usr/local/bin/stalwart`, root:root mode 0755.
Require its full hash and `0.16.15` version. Do not start the permanent unit.

If replacement verification fails, restore only the verified backup and stop.

## Compatibility test

Reuse the committed temporary `PrivateNetwork=yes` recovery architecture and
protected pseudoterminal CLI helper from the prior execution. Generate a fresh
random recovery credential in protected ignored state. Prove the recovery
process has a distinct namespace containing only an up loopback interface, no
non-loopback address, route, veth, bridge, forwarding, NAT, or DNS, and that
port 8080 is unreachable through `192.168.100.199`.

Run `describe` with CLI v1.0.12 inside the same namespace. Render the existing
Git-safe plan from
`config/stalwart/local-foundation.plan.ndjson.template`, substituting only the
four existing protected distinct construction secrets into a private mode-0600
copy. Run `apply --dry-run` and require the exact non-destructive plan shape.

Because dry-run validates schema but not server domain policy, the compatibility
probe is the real apply of the first Domain-only upsert line. Do not include
accounts, listeners, or other objects in that first request.

If `home.arpa` is rejected, record the exact response; do not continue. If it
is accepted, compatibility is proven. Apply only the remaining previously
reviewed operations, query all created objects, remove recovery access, start
the permanent service, and continue the accepted local-only tests. Remote
credential listeners remain prohibited pending trusted TLS.

## Mandatory teardown

Regardless of outcome, stop the recovery process and remove the runtime unit,
root-only recovery environment, transient private plan copy, and temporary
recovery credential. Prove the namespace and port 8080 recovery access are
gone. On rejection leave the permanent service disabled/stopped.

Retain v0.16.15 only when its signature, hash, version, empty-object state,
mount, and stopped service remain healthy. Otherwise restore v0.16.4.

## Outcomes

On acceptance continue the already authorized local foundation and stop at
trusted TLS. On rejection offer only `home.internal` or
`lab.poulin-arpa.com`; do not select either. Never patch Stalwart, bypass its
validator, substitute a domain, expose a public listener, mutate DNS, issue a
certificate, configure Fastmail, or send external mail.

Issue exactly one authorized disposition.
